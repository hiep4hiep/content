comp values(sip) as sip,values(ImpervaAAPercentBlocked) as ImpervaAAPercentBlocked,values(timestamp) as timestamp,values(dhost) as dhost,values(ImpervaAASampleEvents) as ImpervaAASampleEvents,values(account_name) as account_name,values(VID) as VID,values(xff) as xff,values(productsubtype) as productsubtype,values(cpt) as cpt,values(clappsig) as clappsig,values(status) as status,values(COSupport) as COSupport,values(ver) as ver,values(deviceFacility) as deviceFacility,values(act) as act,values(logsubtype) as logsubtype,values(end) as end,values(ImpervaAADominantSiteIds) as ImpervaAADominantSiteIds,values(ImpervaAACves) as ImpervaAACves,values(msg) as msg,values(clapp) as clapp,values(bytes_in) as bytes_in,values(event_message) as event_message,values(event_details) as event_details,values(ImpervaAANumberOfEvents) as ImpervaAANumberOfEvents,values(JavascriptSupport) as JavascriptSupport,values(alert_name) as alert_name,values(suid) as suid,values(cn1) as cn1,values(ImpervaAAAttackType) as ImpervaAAAttackType,values(ccode) as ccode,values(Customer) as Customer,values(RuleInfo) as RuleInfo,values(start) as start,values(request) as request,values(deviceExternalId) as deviceExternalId,values(ImpervaAACountry) as ImpervaAACountry,values(user_email) as user_email,values(ref) as ref,values(app) as app,values(log_type) as log_type,values(latitude) as latitude,values(longitude) as longitude,values(dest) as dest,values(account_id) as account_id,values(cefSeverity) as cefSeverity,values(cicode) as cicode,values(deviceVersion) as deviceVersion,values(requestClientApplication) as requestClientApplication,values(http_method) as http_method,values(dproc) as dproc,values(event_action) as event_action,values(action) as action,values(ImpervaAADominantIps) as ImpervaAADominantIps,values(fileId) as fileId,values(cs9) as cs9,values(sourceServiceName) as sourceServiceName,values(requestMethod) as requestMethod,values(CapSupport) as CapSupport,values(spt) as spt,values(siteid) as siteid,values(cefName) as cefName,values(cefSignature) as cefSignature,values(src) as src,values(user) as user,values(ImpervaAAPlatform) as ImpervaAAPlatform,values(vendorsubtype) as vendorsubtype,values(cs9Label) as cs9Label,values(cefVersion) as cefVersion,values(site_id) as site_id

|alter sip = arrayindex(sip,0),ImpervaAAPercentBlocked = arrayindex(ImpervaAAPercentBlocked,0),timestamp = arrayindex(timestamp,0),dhost = arrayindex(dhost,0),ImpervaAASampleEvents = arrayindex(ImpervaAASampleEvents,0),account_name = arrayindex(account_name,0),VID = arrayindex(VID,0),xff = arrayindex(xff,0),productsubtype = arrayindex(productsubtype,0),cpt = arrayindex(cpt,0),clappsig = arrayindex(clappsig,0),status = arrayindex(status,0),COSupport = arrayindex(COSupport,0),ver = arrayindex(ver,0),deviceFacility = arrayindex(deviceFacility,0),act = arrayindex(act,0),logsubtype = arrayindex(logsubtype,0),end = arrayindex(end,0),ImpervaAADominantSiteIds = arrayindex(ImpervaAADominantSiteIds,0),ImpervaAACves = arrayindex(ImpervaAACves,0),msg = arrayindex(msg,0),clapp = arrayindex(clapp,0),bytes_in = arrayindex(bytes_in,0),event_message = arrayindex(event_message,0),event_details = arrayindex(event_details,0),ImpervaAANumberOfEvents = arrayindex(ImpervaAANumberOfEvents,0),JavascriptSupport = arrayindex(JavascriptSupport,0),alert_name = arrayindex(alert_name,0),suid = arrayindex(suid,0),cn1 = arrayindex(cn1,0),ImpervaAAAttackType = arrayindex(ImpervaAAAttackType,0),ccode = arrayindex(ccode,0),Customer = arrayindex(Customer,0),RuleInfo = arrayindex(RuleInfo,0),start = arrayindex(start,0),request = arrayindex(request,0),deviceExternalId = arrayindex(deviceExternalId,0),ImpervaAACountry = arrayindex(ImpervaAACountry,0),user_email = arrayindex(user_email,0),ref = arrayindex(ref,0),app = arrayindex(app,0),log_type = arrayindex(log_type,0),latitude = arrayindex(latitude,0),longitude = arrayindex(longitude,0),dest = arrayindex(dest,0),account_id = arrayindex(account_id,0),cefSeverity = arrayindex(cefSeverity,0),cicode = arrayindex(cicode,0),deviceVersion = arrayindex(deviceVersion,0),requestClientApplication = arrayindex(requestClientApplication,0),http_method = arrayindex(http_method,0),dproc = arrayindex(dproc,0),event_action = arrayindex(event_action,0),action = arrayindex(action,0),ImpervaAADominantIps = arrayindex(ImpervaAADominantIps,0),fileId = arrayindex(fileId,0),cs9 = arrayindex(cs9,0),sourceServiceName = arrayindex(sourceServiceName,0),requestMethod = arrayindex(requestMethod,0),CapSupport = arrayindex(CapSupport,0),spt = arrayindex(spt,0),siteid = arrayindex(siteid,0),cefName = arrayindex(cefName,0),cefSignature = arrayindex(cefSignature,0),src = arrayindex(src,0),user = arrayindex(user,0),ImpervaAAPlatform = arrayindex(ImpervaAAPlatform,0),vendorsubtype = arrayindex(vendorsubtype,0),cs9Label = arrayindex(cs9Label,0),cefVersion = arrayindex(cefVersion,0),site_id = arrayindex(site_id,0)