comp values(log_timestamp) as log_timestamp,values(log_level) as log_level,values(web_container) as web_container,values(source_ip) as source_ip,values(class) as class,values(tam_id) as tam_id,values(session_id) as session_id,values(x_request_id) as x_request_id,values(message) as message,values(user) as user,values(description) as description,values(dvc_id) as dvc_id,values(event) as event,values(reason) as reason,values(action=if(event) as action=if(event,values(result=if(event) as result=if(event,values(status=if(reason) as status=if(reason,values(url) as url,values(response_code) as response_code,values(uid) as uid,values(creationTime) as creationTime,values(source) as source,values(state) as state,values(consolidationRequired) as consolidationRequired,values(entityRef) as entityRef,values(primaryAccessToken) as primaryAccessToken,values(scvid) as scvid,values(termsAndConditionsAccepted) as termsAndConditionsAccepted,values(lastLoginTime) as lastLoginTime,values(partKeys) as partKeys,values(lockedDetail) as lockedDetail,values(accessTokens) as accessTokens,values(enrolementStatus) as enrolementStatus,values(banknetId) as banknetId,values(passwordReset) as passwordReset,values(cache_control) as cache_control,values(content_language) as content_language,values(content_type) as content_type,values(date) as date,values(Expires) as Expires,values(Pragma) as Pragma,values(strict_transport_security) as strict_transport_security,values(x_content_type_options) as x_content_type_options,values(x_frame_options) as x_frame_options,values(x_powered_by) as x_powered_by,values(x_xss_protection) as x_xss_protection,values(content_length) as content_length,values(Connection) as Connection,values(action) as action

|alter log_timestamp = arrayindex(log_timestamp,0),log_level = arrayindex(log_level,0),web_container = arrayindex(web_container,0),source_ip = arrayindex(source_ip,0),class = arrayindex(class,0),tam_id = arrayindex(tam_id,0),session_id = arrayindex(session_id,0),x_request_id = arrayindex(x_request_id,0),message = arrayindex(message,0),user = arrayindex(user,0),description = arrayindex(description,0),dvc_id = arrayindex(dvc_id,0),event = arrayindex(event,0),reason = arrayindex(reason,0),action=if(event = arrayindex(action=if(event,0),result=if(event = arrayindex(result=if(event,0),status=if(reason = arrayindex(status=if(reason,0),url = arrayindex(url,0),response_code = arrayindex(response_code,0),uid = arrayindex(uid,0),creationTime = arrayindex(creationTime,0),source = arrayindex(source,0),state = arrayindex(state,0),consolidationRequired = arrayindex(consolidationRequired,0),entityRef = arrayindex(entityRef,0),primaryAccessToken = arrayindex(primaryAccessToken,0),scvid = arrayindex(scvid,0),termsAndConditionsAccepted = arrayindex(termsAndConditionsAccepted,0),lastLoginTime = arrayindex(lastLoginTime,0),partKeys = arrayindex(partKeys,0),lockedDetail = arrayindex(lockedDetail,0),accessTokens = arrayindex(accessTokens,0),enrolementStatus = arrayindex(enrolementStatus,0),banknetId = arrayindex(banknetId,0),passwordReset = arrayindex(passwordReset,0),cache_control = arrayindex(cache_control,0),content_language = arrayindex(content_language,0),content_type = arrayindex(content_type,0),date = arrayindex(date,0),Expires = arrayindex(Expires,0),Pragma = arrayindex(Pragma,0),strict_transport_security = arrayindex(strict_transport_security,0),x_content_type_options = arrayindex(x_content_type_options,0),x_frame_options = arrayindex(x_frame_options,0),x_powered_by = arrayindex(x_powered_by,0),x_xss_protection = arrayindex(x_xss_protection,0),content_length = arrayindex(content_length,0),Connection = arrayindex(Connection,0),action = arrayindex(action,0)