comp values(event_type) as event_type,values(action) as action,values(authdevice) as authdevice,values(authenticatorattachment) as authenticatorattachment,values(authmethod) as authmethod,values(cause) as cause,values(devicetype) as devicetype,values(fido2_authenticatordata) as fido2_authenticatordata,values(fido2_clientdatajson) as fido2_clientdatajson,values(fido2_credentialid) as fido2_credentialid,values(fido2_publickey) as fido2_publickey,values(fido2_relyingparty) as fido2_relyingparty,values(fido2_signature) as fido2_signature,values(origin) as origin,values(realm) as realm,values(result) as result,values(sourcetype) as sourcetype,values(subject) as subject,values(subtype) as subtype,values(targetid) as targetid,values(username) as username,values(event_type) as event_type,values(applicationid) as applicationid,values(applicationname) as applicationname,values(applicationtype) as applicationtype,values(at_hash) as at_hash,values(cause) as cause,values(client_id) as client_id,values(client_name) as client_name,values(client_type) as client_type,values(devicetype) as devicetype,values(grant_id) as grant_id,values(grant_type) as grant_type,values(id_token) as id_token,values(origin) as origin,values(realm) as realm,values(redirecturl) as redirecturl,values(result) as result,values(scope) as scope,values(sessionid) as sessionid,values(subtype) as subtype,values(uasessionid) as uasessionid,values(userid) as userid,values(username) as username,values(event_type) as event_type,values(access_token_type) as access_token_type,values(action) as action,values(applicationid) as applicationid,values(applicationname) as applicationname,values(applicationtype) as applicationtype,values(at_hash) as at_hash,values(client_id) as client_id,values(client_name) as client_name,values(client_category) as client_category,values(devicetype) as devicetype,values(grant_id) as grant_id,values(grant_type) as grant_type,values(origin) as origin,values(result) as result,values(scope) as scope,values(token_lifetime) as token_lifetime,values(token_type) as token_type,values(event_type) as event_type,values(action) as action,values(api_grant_type) as api_grant_type,values(cause) as cause,values(devicetype) as devicetype,values(json) as json,values(location) as location,values(messageid) as messageid,values(modified) as modified,values(performedby) as performedby,values(performedby_type) as performedby_type,values(realm) as realm,values(resource) as resource,values(result) as result,values(self) as self,values(targetname) as targetname,values(targetid) as targetid,values(targetid_realm) as targetid_realm,values(targetid_username) as targetid_username,values(userinfo_lookup) as userinfo_lookup

|alter event_type = arrayindex(event_type,0),action = arrayindex(action,0),authdevice = arrayindex(authdevice,0),authenticatorattachment = arrayindex(authenticatorattachment,0),authmethod = arrayindex(authmethod,0),cause = arrayindex(cause,0),devicetype = arrayindex(devicetype,0),fido2_authenticatordata = arrayindex(fido2_authenticatordata,0),fido2_clientdatajson = arrayindex(fido2_clientdatajson,0),fido2_credentialid = arrayindex(fido2_credentialid,0),fido2_publickey = arrayindex(fido2_publickey,0),fido2_relyingparty = arrayindex(fido2_relyingparty,0),fido2_signature = arrayindex(fido2_signature,0),origin = arrayindex(origin,0),realm = arrayindex(realm,0),result = arrayindex(result,0),sourcetype = arrayindex(sourcetype,0),subject = arrayindex(subject,0),subtype = arrayindex(subtype,0),targetid = arrayindex(targetid,0),username = arrayindex(username,0),event_type = arrayindex(event_type,0),applicationid = arrayindex(applicationid,0),applicationname = arrayindex(applicationname,0),applicationtype = arrayindex(applicationtype,0),at_hash = arrayindex(at_hash,0),cause = arrayindex(cause,0),client_id = arrayindex(client_id,0),client_name = arrayindex(client_name,0),client_type = arrayindex(client_type,0),devicetype = arrayindex(devicetype,0),grant_id = arrayindex(grant_id,0),grant_type = arrayindex(grant_type,0),id_token = arrayindex(id_token,0),origin = arrayindex(origin,0),realm = arrayindex(realm,0),redirecturl = arrayindex(redirecturl,0),result = arrayindex(result,0),scope = arrayindex(scope,0),sessionid = arrayindex(sessionid,0),subtype = arrayindex(subtype,0),uasessionid = arrayindex(uasessionid,0),userid = arrayindex(userid,0),username = arrayindex(username,0),event_type = arrayindex(event_type,0),access_token_type = arrayindex(access_token_type,0),action = arrayindex(action,0),applicationid = arrayindex(applicationid,0),applicationname = arrayindex(applicationname,0),applicationtype = arrayindex(applicationtype,0),at_hash = arrayindex(at_hash,0),client_id = arrayindex(client_id,0),client_name = arrayindex(client_name,0),client_category = arrayindex(client_category,0),devicetype = arrayindex(devicetype,0),grant_id = arrayindex(grant_id,0),grant_type = arrayindex(grant_type,0),origin = arrayindex(origin,0),result = arrayindex(result,0),scope = arrayindex(scope,0),token_lifetime = arrayindex(token_lifetime,0),token_type = arrayindex(token_type,0),event_type = arrayindex(event_type,0),action = arrayindex(action,0),api_grant_type = arrayindex(api_grant_type,0),cause = arrayindex(cause,0),devicetype = arrayindex(devicetype,0),json = arrayindex(json,0),location = arrayindex(location,0),messageid = arrayindex(messageid,0),modified = arrayindex(modified,0),performedby = arrayindex(performedby,0),performedby_type = arrayindex(performedby_type,0),realm = arrayindex(realm,0),resource = arrayindex(resource,0),result = arrayindex(result,0),self = arrayindex(self,0),targetname = arrayindex(targetname,0),targetid = arrayindex(targetid,0),targetid_realm = arrayindex(targetid_realm,0),targetid_username = arrayindex(targetid_username,0),userinfo_lookup = arrayindex(userinfo_lookup,0)