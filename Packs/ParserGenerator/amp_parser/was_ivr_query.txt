comp values(log_timestamp) as log_timestamp,values(log_level) as log_level,values(WebContainer) as WebContainer,values(api_endpoint) as api_endpoint,values(remote_ip) as remote_ip,values(x_request_id) as x_request_id,values(auth_url) as auth_url,values(message) as message,values(signature) as signature,values(status_code) as status_code,values(action) as action,values(status) as status,values(json_data) as json_data,values(message) as message,values(message_json_data) as message_json_data,values(exception) as exception,values(status) as status,values(c2app) as c2app,values(c2subapp) as c2subapp,values(c2env) as c2env,values(c2ttl) as c2ttl,values(c2sec) as c2sec,values(client_addr) as client_addr,values(source_ip) as source_ip,values(iv_user) as iv_user,values(method) as method,values(host) as host,values(request) as request,values(junction) as junction,values(response_code) as response_code,values(sent_byte) as sent_byte,values(response_time_s) as response_time_s,values(response_time_m) as response_time_m,values(x_request_id) as x_request_id,values(message) as message,values(threat_name) as threat_name,values(auth_url) as auth_url,values(remote_ip) as remote_ip,values(message_json_data) as message_json_data,values(WebContainer) as WebContainer,values(signature) as signature

|alter log_timestamp = arrayindex(log_timestamp,0),log_level = arrayindex(log_level,0),WebContainer = arrayindex(WebContainer,0),api_endpoint = arrayindex(api_endpoint,0),remote_ip = arrayindex(remote_ip,0),x_request_id = arrayindex(x_request_id,0),auth_url = arrayindex(auth_url,0),message = arrayindex(message,0),signature = arrayindex(signature,0),status_code = arrayindex(status_code,0),action = arrayindex(action,0),status = arrayindex(status,0),json_data = arrayindex(json_data,0),message = arrayindex(message,0),message_json_data = arrayindex(message_json_data,0),exception = arrayindex(exception,0),status = arrayindex(status,0),c2app = arrayindex(c2app,0),c2subapp = arrayindex(c2subapp,0),c2env = arrayindex(c2env,0),c2ttl = arrayindex(c2ttl,0),c2sec = arrayindex(c2sec,0),client_addr = arrayindex(client_addr,0),source_ip = arrayindex(source_ip,0),iv_user = arrayindex(iv_user,0),method = arrayindex(method,0),host = arrayindex(host,0),request = arrayindex(request,0),junction = arrayindex(junction,0),response_code = arrayindex(response_code,0),sent_byte = arrayindex(sent_byte,0),response_time_s = arrayindex(response_time_s,0),response_time_m = arrayindex(response_time_m,0),x_request_id = arrayindex(x_request_id,0),message = arrayindex(message,0),threat_name = arrayindex(threat_name,0),auth_url = arrayindex(auth_url,0),remote_ip = arrayindex(remote_ip,0),message_json_data = arrayindex(message_json_data,0),WebContainer = arrayindex(WebContainer,0),signature = arrayindex(signature,0)