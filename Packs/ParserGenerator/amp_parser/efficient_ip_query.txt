comp values(event_time) as event_time,values(event_type) as event_type,values(event_message) as event_message,values(dhcp_message_type) as dhcp_message_type,values(source_mac) as source_mac,values(source_ip_relay) as source_ip_relay,values(dhcp_message) as dhcp_message,values(source_ip_request) as source_ip_request,values(source_device) as source_device,values(source_ip_ack) as source_ip_ack,values(dns_source_ip) as dns_source_ip,values(dns_source_port) as dns_source_port,values(dns_message) as dns_message

|alter event_time = arrayindex(event_time,0),event_type = arrayindex(event_type,0),event_message = arrayindex(event_message,0),dhcp_message_type = arrayindex(dhcp_message_type,0),source_mac = arrayindex(source_mac,0),source_ip_relay = arrayindex(source_ip_relay,0),dhcp_message = arrayindex(dhcp_message,0),source_ip_request = arrayindex(source_ip_request,0),source_device = arrayindex(source_device,0),source_ip_ack = arrayindex(source_ip_ack,0),dns_source_ip = arrayindex(dns_source_ip,0),dns_source_port = arrayindex(dns_source_port,0),dns_message = arrayindex(dns_message,0)