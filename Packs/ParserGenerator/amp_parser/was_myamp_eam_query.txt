comp values(log_timestamp) as log_timestamp,values(header_fields) as header_fields,values(header3) as header3,values(event) as event,values(c_ip) as c_ip,values(SocketException) as SocketException,values(ResourceAccessException) as ResourceAccessException,values(CertPathValidatorException) as CertPathValidatorException,values(status) as status,values(status2) as status2,values(url) as url,values(response_time) as response_time,values(user_id) as user_id,values(PartyKey) as PartyKey,values(dvc) as dvc,values(Country) as Country,values(channel) as channel,values(message_correlation_id) as message_correlation_id,values(EntityReference) as EntityReference,values(response_code) as response_code,values(signature) as signature,values(app) as app,values(was_server) as was_server,values(failedlogin) as failedlogin,values(source_file) as source_file,values(dest) as dest

|alter log_timestamp = arrayindex(log_timestamp,0),header_fields = arrayindex(header_fields,0),header3 = arrayindex(header3,0),event = arrayindex(event,0),c_ip = arrayindex(c_ip,0),SocketException = arrayindex(SocketException,0),ResourceAccessException = arrayindex(ResourceAccessException,0),CertPathValidatorException = arrayindex(CertPathValidatorException,0),status = arrayindex(status,0),status2 = arrayindex(status2,0),url = arrayindex(url,0),response_time = arrayindex(response_time,0),user_id = arrayindex(user_id,0),PartyKey = arrayindex(PartyKey,0),dvc = arrayindex(dvc,0),Country = arrayindex(Country,0),channel = arrayindex(channel,0),message_correlation_id = arrayindex(message_correlation_id,0),EntityReference = arrayindex(EntityReference,0),response_code = arrayindex(response_code,0),signature = arrayindex(signature,0),app = arrayindex(app,0),was_server = arrayindex(was_server,0),failedlogin = arrayindex(failedlogin,0),source_file = arrayindex(source_file,0),dest = arrayindex(dest,0)