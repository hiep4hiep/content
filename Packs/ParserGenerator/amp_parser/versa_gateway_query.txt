comp values(dnsfBadResolvedV4Addr) as dnsfBadResolvedV4Addr,values(ipsProfile) as ipsProfile,values(siteType) as siteType,values(avMalwareType) as avMalwareType,values(dstLocation) as dstLocation,values(dnsfDomainReputation) as dnsfDomainReputation,values(applianceId) as applianceId,values(match) as match,values(srcLocation) as srcLocation,values(fromZone) as fromZone,values(ingressInterfaceName) as ingressInterfaceName,values(rInst) as rInst,values(fileHashType) as fileHashType,values(subscriptionType) as subscriptionType,values(alarmSeverity) as alarmSeverity,values(mstatsAttribs) as mstatsAttribs,values(logsubtype) as logsubtype,values(destinationPort) as destinationPort,values(longitude) as longitude,values(dnsfDetectionReason) as dnsfDetectionReason,values(signatureId) as signatureId,values(svcUptime) as svcUptime,values(sourceTransportPort) as sourceTransportPort,values(fileSize) as fileSize,values(destinationTransportPort) as destinationTransportPort,values(signaturePriority) as signaturePriority,values(alarmClearable) as alarmClearable,values(dnsfAction) as dnsfAction,values(appIdStr) as appIdStr,values(authMethod) as authMethod,values(transactionId) as transactionId,values(alarmOwner) as alarmOwner,values(ipsApplication) as ipsApplication,values(ipsDirection) as ipsDirection,values(secAccRuleAction) as secAccRuleAction,values(fwdClass) as fwdClass,values(accCktName) as accCktName,values(mstatsTotSentOctets) as mstatsTotSentOctets,values(mstatsType) as mstatsType,values(event_type) as event_type,values(location) as location,values(dnsfRuleName) as dnsfRuleName,values(tenantId) as tenantId,values(generateTime) as generateTime,values(toCountry) as toCountry,values(alarmText) as alarmText,values(urlfActionMessage) as urlfActionMessage,values(threatSeverity) as threatSeverity,values(downlinkBw) as downlinkBw,values(sentPackets) as sentPackets,values(srcDomain) as srcDomain,values(appSubFamily) as appSubFamily,values(fromUser) as fromUser,values(fileName) as fileName,values(fileTransDir) as fileTransDir,values(egressInterfaceName) as egressInterfaceName,values(siteList) as siteList,values(ipsProfileRule) as ipsProfileRule,values(siteCount) as siteCount,values(signatureMsg) as signatureMsg,values(dnsfProfileName) as dnsfProfileName,values(successfulAttempts) as successfulAttempts,values(avAccuracy) as avAccuracy,values(srcBlackList) as srcBlackList,values(dnsfDomainCategory) as dnsfDomainCategory,values(rule) as rule,values(dnsfBadCname) as dnsfBadCname,values(pathsUp) as pathsUp,values(alarmKey) as alarmKey,values(dstWhiteList) as dstWhiteList,values(recvdPackets) as recvdPackets,values(eventSetCntr) as eventSetCntr,values(racIP) as racIP,values(httpUrl) as httpUrl,values(rmtSitesDown) as rmtSitesDown,values(localAccCktId) as localAccCktId,values(delay) as delay,values(fwdLossRatio) as fwdLossRatio,values(mstatsTimeBlock) as mstatsTimeBlock,values(srcWhiteList) as srcWhiteList,values(dnsfDetectedDomain) as dnsfDetectedDomain,values(classMsg) as classMsg,values(cktType) as cktType,values(srcReputation) as srcReputation,values(accCktId) as accCktId,values(localAccCktName) as localAccCktName,values(mstatsTotRecvdOctets) as mstatsTotRecvdOctets,values(deviceKey) as deviceKey,values(revDelayVar) as revDelayVar,values(protocolIdentifier) as protocolIdentifier,values(latitude) as latitude,values(destSGT) as destSGT,values(failedAttempts) as failedAttempts,values(pathsNoConf) as pathsNoConf,values(recvdOctets) as recvdOctets,values(urlReputation) as urlReputation,values(fileType) as fileType,values(revLossRatio) as revLossRatio,values(reverseForwardingClass) as reverseForwardingClass,values(dstBlackList) as dstBlackList,values(secAccRuleName) as secAccRuleName,values(fileHashValue) as fileHashValue,values(fileSizeExceed) as fileSizeExceed,values(remoteSiteName) as remoteSiteName,values(eventClrCntr) as eventClrCntr,values(authProfile) as authProfile,values(racAccessType) as racAccessType,values(roundTripTime) as roundTripTime,values(eventType) as eventType,values(avMalwareName) as avMalwareName,values(avAction) as avAction,values(siteName) as siteName,values(uplinkBw) as uplinkBw,values(ipsProtocol) as ipsProtocol,values(mstatsTotSessCount) as mstatsTotSessCount,values(forwardForwardingClass) as forwardForwardingClass,values(fwdLoss) as fwdLoss,values(racTxBytes) as racTxBytes,values(dstDomain) as dstDomain,values(sourcePort) as sourcePort,values(duration) as duration,values(racTunnelIP) as racTunnelIP,values(moduleId) as moduleId,values(allRmtSitesDownTime) as allRmtSitesDownTime,values(dnsfDetectionType) as dnsfDetectionType,values(appFamily) as appFamily,values(remoteAccCktName) as remoteAccCktName,values(siteStatus) as siteStatus,values(dnsfSinkHoleIp) as dnsfSinkHoleIp,values(urlCategory) as urlCategory,values(osVersion) as osVersion,values(sentOctets) as sentOctets,values(webSessCnt) as webSessCnt,values(revSent) as revSent,values(localSiteId) as localSiteId,values(concurrentUsers) as concurrentUsers,values(delayVar) as delayVar,values(authStatusMessage) as authStatusMessage,values(localSiteName) as localSiteName,values(destinationIPv4Address) as destinationIPv4Address,values(action) as action,values(dnsSessCnt) as dnsSessCnt,values(longSessCnt) as longSessCnt,values(event_time) as event_time,values(deviceName) as deviceName,values(packetTime) as packetTime,values(dnsfBadResolvedV6Addr) as dnsfBadResolvedV6Addr,values(secAccClientVersion) as secAccClientVersion,values(urlfProfile) as urlfProfile,values(profileName) as profileName,values(failureReason) as failureReason,values(vsnId) as vsnId,values(dnsfIpReputation) as dnsfIpReputation,values(racRxBytes) as racRxBytes,values(flowEndMilliseconds) as flowEndMilliseconds,values(lastState) as lastState,values(secAccProfName) as secAccProfName,values(fwdDelayVar) as fwdDelayVar,values(eipProfileName) as eipProfileName,values(dnsfDetectionTime) as dnsfDetectionTime,values(allLclCktDown) as allLclCktDown,values(dnsfTunnelClientIp) as dnsfTunnelClientIp,values(pduLossRatio) as pduLossRatio,values(alarmClass) as alarmClass,values(fileRuleName) as fileRuleName,values(flowId) as flowId,values(fromCountry) as fromCountry,values(icmpTypeIPv4) as icmpTypeIPv4,values(urlfAction) as urlfAction,values(tenantName) as tenantName,values(dnsfIpGeoLocation) as dnsfIpGeoLocation,values([INGEST:vendor) as [INGEST:vendor,values(allRmtSitesDown) as allRmtSitesDown,values(ipfActionMessage) as ipfActionMessage,values(remoteSiteId) as remoteSiteId,values(appProductivity) as appProductivity,values(appId) as appId,values(ipfAction) as ipfAction,values(authStatus) as authStatus,values(serialNum) as serialNum,values(threatType) as threatType,values(fileFoundIn) as fileFoundIn,values(lossRatio) as lossRatio,values(creationTime) as creationTime,values(appRisk) as appRisk,values(siteId) as siteId,values(alarmType) as alarmType,values(applianceName) as applianceName,values(fwdSent) as fwdSent,values(dnsfEvType) as dnsfEvType,values(sessCnt) as sessCnt,values(HitCount) as HitCount,values(accCktBW) as accCktBW,values(alarmEventType) as alarmEventType,values(dstReputation) as dstReputation,values(mstatsTotSessDuration) as mstatsTotSessDuration,values(localAccCktIp) as localAccCktIp,values(revLoss) as revLoss,values(isp) as isp,values(sourceIPv4Address) as sourceIPv4Address,values(userName) as userName,values(srcSGT) as srcSGT,values(racEventType) as racEventType,values(alarmSeqNo) as alarmSeqNo,values(host) as host,values(idpAction) as idpAction,values(flowCookie) as flowCookie,values(authTime) as authTime,values(rmtSitesUp) as rmtSitesUp,values(alarmCause) as alarmCause,values(traffScope) as traffScope,values(fileFilterAction) as fileFilterAction,values(toZone) as toZone,values(dnsfMsgType) as dnsfMsgType,values(dnsfDomain) as dnsfDomain,values(flowStartMilliseconds) as flowStartMilliseconds,values(groupId) as groupId,values(vpnProfile) as vpnProfile,values(pathsDown) as pathsDown,values(cktMedia) as cktMedia,values(signatureRev) as signatureRev,values(alarmKind) as alarmKind,values(uptime) as uptime,values(os) as os,values(cktProvider) as cktProvider

|alter dnsfBadResolvedV4Addr = arrayindex(dnsfBadResolvedV4Addr,0),ipsProfile = arrayindex(ipsProfile,0),siteType = arrayindex(siteType,0),avMalwareType = arrayindex(avMalwareType,0),dstLocation = arrayindex(dstLocation,0),dnsfDomainReputation = arrayindex(dnsfDomainReputation,0),applianceId = arrayindex(applianceId,0),match = arrayindex(match,0),srcLocation = arrayindex(srcLocation,0),fromZone = arrayindex(fromZone,0),ingressInterfaceName = arrayindex(ingressInterfaceName,0),rInst = arrayindex(rInst,0),fileHashType = arrayindex(fileHashType,0),subscriptionType = arrayindex(subscriptionType,0),alarmSeverity = arrayindex(alarmSeverity,0),mstatsAttribs = arrayindex(mstatsAttribs,0),logsubtype = arrayindex(logsubtype,0),destinationPort = arrayindex(destinationPort,0),longitude = arrayindex(longitude,0),dnsfDetectionReason = arrayindex(dnsfDetectionReason,0),signatureId = arrayindex(signatureId,0),svcUptime = arrayindex(svcUptime,0),sourceTransportPort = arrayindex(sourceTransportPort,0),fileSize = arrayindex(fileSize,0),destinationTransportPort = arrayindex(destinationTransportPort,0),signaturePriority = arrayindex(signaturePriority,0),alarmClearable = arrayindex(alarmClearable,0),dnsfAction = arrayindex(dnsfAction,0),appIdStr = arrayindex(appIdStr,0),authMethod = arrayindex(authMethod,0),transactionId = arrayindex(transactionId,0),alarmOwner = arrayindex(alarmOwner,0),ipsApplication = arrayindex(ipsApplication,0),ipsDirection = arrayindex(ipsDirection,0),secAccRuleAction = arrayindex(secAccRuleAction,0),fwdClass = arrayindex(fwdClass,0),accCktName = arrayindex(accCktName,0),mstatsTotSentOctets = arrayindex(mstatsTotSentOctets,0),mstatsType = arrayindex(mstatsType,0),event_type = arrayindex(event_type,0),location = arrayindex(location,0),dnsfRuleName = arrayindex(dnsfRuleName,0),tenantId = arrayindex(tenantId,0),generateTime = arrayindex(generateTime,0),toCountry = arrayindex(toCountry,0),alarmText = arrayindex(alarmText,0),urlfActionMessage = arrayindex(urlfActionMessage,0),threatSeverity = arrayindex(threatSeverity,0),downlinkBw = arrayindex(downlinkBw,0),sentPackets = arrayindex(sentPackets,0),srcDomain = arrayindex(srcDomain,0),appSubFamily = arrayindex(appSubFamily,0),fromUser = arrayindex(fromUser,0),fileName = arrayindex(fileName,0),fileTransDir = arrayindex(fileTransDir,0),egressInterfaceName = arrayindex(egressInterfaceName,0),siteList = arrayindex(siteList,0),ipsProfileRule = arrayindex(ipsProfileRule,0),siteCount = arrayindex(siteCount,0),signatureMsg = arrayindex(signatureMsg,0),dnsfProfileName = arrayindex(dnsfProfileName,0),successfulAttempts = arrayindex(successfulAttempts,0),avAccuracy = arrayindex(avAccuracy,0),srcBlackList = arrayindex(srcBlackList,0),dnsfDomainCategory = arrayindex(dnsfDomainCategory,0),rule = arrayindex(rule,0),dnsfBadCname = arrayindex(dnsfBadCname,0),pathsUp = arrayindex(pathsUp,0),alarmKey = arrayindex(alarmKey,0),dstWhiteList = arrayindex(dstWhiteList,0),recvdPackets = arrayindex(recvdPackets,0),eventSetCntr = arrayindex(eventSetCntr,0),racIP = arrayindex(racIP,0),httpUrl = arrayindex(httpUrl,0),rmtSitesDown = arrayindex(rmtSitesDown,0),localAccCktId = arrayindex(localAccCktId,0),delay = arrayindex(delay,0),fwdLossRatio = arrayindex(fwdLossRatio,0),mstatsTimeBlock = arrayindex(mstatsTimeBlock,0),srcWhiteList = arrayindex(srcWhiteList,0),dnsfDetectedDomain = arrayindex(dnsfDetectedDomain,0),classMsg = arrayindex(classMsg,0),cktType = arrayindex(cktType,0),srcReputation = arrayindex(srcReputation,0),accCktId = arrayindex(accCktId,0),localAccCktName = arrayindex(localAccCktName,0),mstatsTotRecvdOctets = arrayindex(mstatsTotRecvdOctets,0),deviceKey = arrayindex(deviceKey,0),revDelayVar = arrayindex(revDelayVar,0),protocolIdentifier = arrayindex(protocolIdentifier,0),latitude = arrayindex(latitude,0),destSGT = arrayindex(destSGT,0),failedAttempts = arrayindex(failedAttempts,0),pathsNoConf = arrayindex(pathsNoConf,0),recvdOctets = arrayindex(recvdOctets,0),urlReputation = arrayindex(urlReputation,0),fileType = arrayindex(fileType,0),revLossRatio = arrayindex(revLossRatio,0),reverseForwardingClass = arrayindex(reverseForwardingClass,0),dstBlackList = arrayindex(dstBlackList,0),secAccRuleName = arrayindex(secAccRuleName,0),fileHashValue = arrayindex(fileHashValue,0),fileSizeExceed = arrayindex(fileSizeExceed,0),remoteSiteName = arrayindex(remoteSiteName,0),eventClrCntr = arrayindex(eventClrCntr,0),authProfile = arrayindex(authProfile,0),racAccessType = arrayindex(racAccessType,0),roundTripTime = arrayindex(roundTripTime,0),eventType = arrayindex(eventType,0),avMalwareName = arrayindex(avMalwareName,0),avAction = arrayindex(avAction,0),siteName = arrayindex(siteName,0),uplinkBw = arrayindex(uplinkBw,0),ipsProtocol = arrayindex(ipsProtocol,0),mstatsTotSessCount = arrayindex(mstatsTotSessCount,0),forwardForwardingClass = arrayindex(forwardForwardingClass,0),fwdLoss = arrayindex(fwdLoss,0),racTxBytes = arrayindex(racTxBytes,0),dstDomain = arrayindex(dstDomain,0),sourcePort = arrayindex(sourcePort,0),duration = arrayindex(duration,0),racTunnelIP = arrayindex(racTunnelIP,0),moduleId = arrayindex(moduleId,0),allRmtSitesDownTime = arrayindex(allRmtSitesDownTime,0),dnsfDetectionType = arrayindex(dnsfDetectionType,0),appFamily = arrayindex(appFamily,0),remoteAccCktName = arrayindex(remoteAccCktName,0),siteStatus = arrayindex(siteStatus,0),dnsfSinkHoleIp = arrayindex(dnsfSinkHoleIp,0),urlCategory = arrayindex(urlCategory,0),osVersion = arrayindex(osVersion,0),sentOctets = arrayindex(sentOctets,0),webSessCnt = arrayindex(webSessCnt,0),revSent = arrayindex(revSent,0),localSiteId = arrayindex(localSiteId,0),concurrentUsers = arrayindex(concurrentUsers,0),delayVar = arrayindex(delayVar,0),authStatusMessage = arrayindex(authStatusMessage,0),localSiteName = arrayindex(localSiteName,0),destinationIPv4Address = arrayindex(destinationIPv4Address,0),action = arrayindex(action,0),dnsSessCnt = arrayindex(dnsSessCnt,0),longSessCnt = arrayindex(longSessCnt,0),event_time = arrayindex(event_time,0),deviceName = arrayindex(deviceName,0),packetTime = arrayindex(packetTime,0),dnsfBadResolvedV6Addr = arrayindex(dnsfBadResolvedV6Addr,0),secAccClientVersion = arrayindex(secAccClientVersion,0),urlfProfile = arrayindex(urlfProfile,0),profileName = arrayindex(profileName,0),failureReason = arrayindex(failureReason,0),vsnId = arrayindex(vsnId,0),dnsfIpReputation = arrayindex(dnsfIpReputation,0),racRxBytes = arrayindex(racRxBytes,0),flowEndMilliseconds = arrayindex(flowEndMilliseconds,0),lastState = arrayindex(lastState,0),secAccProfName = arrayindex(secAccProfName,0),fwdDelayVar = arrayindex(fwdDelayVar,0),eipProfileName = arrayindex(eipProfileName,0),dnsfDetectionTime = arrayindex(dnsfDetectionTime,0),allLclCktDown = arrayindex(allLclCktDown,0),dnsfTunnelClientIp = arrayindex(dnsfTunnelClientIp,0),pduLossRatio = arrayindex(pduLossRatio,0),alarmClass = arrayindex(alarmClass,0),fileRuleName = arrayindex(fileRuleName,0),flowId = arrayindex(flowId,0),fromCountry = arrayindex(fromCountry,0),icmpTypeIPv4 = arrayindex(icmpTypeIPv4,0),urlfAction = arrayindex(urlfAction,0),tenantName = arrayindex(tenantName,0),dnsfIpGeoLocation = arrayindex(dnsfIpGeoLocation,0),[INGEST:vendor = arrayindex([INGEST:vendor,0),allRmtSitesDown = arrayindex(allRmtSitesDown,0),ipfActionMessage = arrayindex(ipfActionMessage,0),remoteSiteId = arrayindex(remoteSiteId,0),appProductivity = arrayindex(appProductivity,0),appId = arrayindex(appId,0),ipfAction = arrayindex(ipfAction,0),authStatus = arrayindex(authStatus,0),serialNum = arrayindex(serialNum,0),threatType = arrayindex(threatType,0),fileFoundIn = arrayindex(fileFoundIn,0),lossRatio = arrayindex(lossRatio,0),creationTime = arrayindex(creationTime,0),appRisk = arrayindex(appRisk,0),siteId = arrayindex(siteId,0),alarmType = arrayindex(alarmType,0),applianceName = arrayindex(applianceName,0),fwdSent = arrayindex(fwdSent,0),dnsfEvType = arrayindex(dnsfEvType,0),sessCnt = arrayindex(sessCnt,0),HitCount = arrayindex(HitCount,0),accCktBW = arrayindex(accCktBW,0),alarmEventType = arrayindex(alarmEventType,0),dstReputation = arrayindex(dstReputation,0),mstatsTotSessDuration = arrayindex(mstatsTotSessDuration,0),localAccCktIp = arrayindex(localAccCktIp,0),revLoss = arrayindex(revLoss,0),isp = arrayindex(isp,0),sourceIPv4Address = arrayindex(sourceIPv4Address,0),userName = arrayindex(userName,0),srcSGT = arrayindex(srcSGT,0),racEventType = arrayindex(racEventType,0),alarmSeqNo = arrayindex(alarmSeqNo,0),host = arrayindex(host,0),idpAction = arrayindex(idpAction,0),flowCookie = arrayindex(flowCookie,0),authTime = arrayindex(authTime,0),rmtSitesUp = arrayindex(rmtSitesUp,0),alarmCause = arrayindex(alarmCause,0),traffScope = arrayindex(traffScope,0),fileFilterAction = arrayindex(fileFilterAction,0),toZone = arrayindex(toZone,0),dnsfMsgType = arrayindex(dnsfMsgType,0),dnsfDomain = arrayindex(dnsfDomain,0),flowStartMilliseconds = arrayindex(flowStartMilliseconds,0),groupId = arrayindex(groupId,0),vpnProfile = arrayindex(vpnProfile,0),pathsDown = arrayindex(pathsDown,0),cktMedia = arrayindex(cktMedia,0),signatureRev = arrayindex(signatureRev,0),alarmKind = arrayindex(alarmKind,0),uptime = arrayindex(uptime,0),os = arrayindex(os,0),cktProvider = arrayindex(cktProvider,0)