comp values(log_timestamp) as log_timestamp,values(client_ip) as client_ip,values(x_forwarded_host) as x_forwarded_host,values(web_container) as web_container,values(auditor) as auditor,values(user_id) as user_id,values(x_request_id) as x_request_id,values(event_type) as event_type,values(reason) as reason,values(scv_id) as scv_id,values(ldap_object) as ldap_object

|alter log_timestamp = arrayindex(log_timestamp,0),client_ip = arrayindex(client_ip,0),x_forwarded_host = arrayindex(x_forwarded_host,0),web_container = arrayindex(web_container,0),auditor = arrayindex(auditor,0),user_id = arrayindex(user_id,0),x_request_id = arrayindex(x_request_id,0),event_type = arrayindex(event_type,0),reason = arrayindex(reason,0),scv_id = arrayindex(scv_id,0),ldap_object = arrayindex(ldap_object,0)