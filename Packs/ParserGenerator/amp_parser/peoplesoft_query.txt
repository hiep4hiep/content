comp values(action) as action,values(reason) as reason,values(user_agent) as user_agent,values(authentication_service) as authentication_service,values(OperatorID) as OperatorID,values(host) as host,values(app) as app,values(SRID) as SRID,values(user) as user,values(Message) as Message,values(authentication_method) as authentication_method,values(dest_ip) as dest_ip,values(src) as src,values(src_user) as src_user,values(dest) as dest,values(multipleFields) as multipleFields,values(signature) as signature

|alter action = arrayindex(action,0),reason = arrayindex(reason,0),user_agent = arrayindex(user_agent,0),authentication_service = arrayindex(authentication_service,0),OperatorID = arrayindex(OperatorID,0),host = arrayindex(host,0),app = arrayindex(app,0),SRID = arrayindex(SRID,0),user = arrayindex(user,0),Message = arrayindex(Message,0),authentication_method = arrayindex(authentication_method,0),dest_ip = arrayindex(dest_ip,0),src = arrayindex(src,0),src_user = arrayindex(src_user,0),dest = arrayindex(dest,0),multipleFields = arrayindex(multipleFields,0),signature = arrayindex(signature,0)