comp values(action) as action,values(command) as command,values(change_type) as change_type,values(src_user) as src_user,values(allfields) as allfields,values(src_user_name) as src_user_name,values(src_ip) as src_ip,values(UserAgent) as UserAgent,values(ClientInformation) as ClientInformation,values(result) as result,values(date_year) as date_year,values(date_zone) as date_zone,values(date_second) as date_second,values(result_id) as result_id,values(dest) as dest,values(Success) as Success,values(date) as date,values(vendor_product) as vendor_product,values(reason) as reason,values(UserHostAddress) as UserHostAddress,values(IPAddress) as IPAddress,values(date_month) as date_month,values(cvcv) as cvcv,values(app) as app,values(signature) as signature,values(date_hour) as date_hour,values(status) as status,values(UserAuditId) as UserAuditId,values(user) as user,values(EffectiveDate) as EffectiveDate,values(NorthDeviceToken) as NorthDeviceToken,values(date_minute) as date_minute,values(array) as array,values(date_wday) as date_wday,values(XForwardedFor) as XForwardedFor,values(Message) as Message,values(DeviceInformation) as DeviceInformation,values(tag_action) as tag_action,values(AuditType) as AuditType,values(date_mday) as date_mday,values(date_round_to_second) as date_round_to_second,values(smf) as smf,values(src) as src,values(UserID) as UserID

|alter action = arrayindex(action,0),command = arrayindex(command,0),change_type = arrayindex(change_type,0),src_user = arrayindex(src_user,0),allfields = arrayindex(allfields,0),src_user_name = arrayindex(src_user_name,0),src_ip = arrayindex(src_ip,0),UserAgent = arrayindex(UserAgent,0),ClientInformation = arrayindex(ClientInformation,0),result = arrayindex(result,0),date_year = arrayindex(date_year,0),date_zone = arrayindex(date_zone,0),date_second = arrayindex(date_second,0),result_id = arrayindex(result_id,0),dest = arrayindex(dest,0),Success = arrayindex(Success,0),date = arrayindex(date,0),vendor_product = arrayindex(vendor_product,0),reason = arrayindex(reason,0),UserHostAddress = arrayindex(UserHostAddress,0),IPAddress = arrayindex(IPAddress,0),date_month = arrayindex(date_month,0),cvcv = arrayindex(cvcv,0),app = arrayindex(app,0),signature = arrayindex(signature,0),date_hour = arrayindex(date_hour,0),status = arrayindex(status,0),UserAuditId = arrayindex(UserAuditId,0),user = arrayindex(user,0),EffectiveDate = arrayindex(EffectiveDate,0),NorthDeviceToken = arrayindex(NorthDeviceToken,0),date_minute = arrayindex(date_minute,0),array = arrayindex(array,0),date_wday = arrayindex(date_wday,0),XForwardedFor = arrayindex(XForwardedFor,0),Message = arrayindex(Message,0),DeviceInformation = arrayindex(DeviceInformation,0),tag_action = arrayindex(tag_action,0),AuditType = arrayindex(AuditType,0),date_mday = arrayindex(date_mday,0),date_round_to_second = arrayindex(date_round_to_second,0),smf = arrayindex(smf,0),src = arrayindex(src,0),UserID = arrayindex(UserID,0)