comp values(message_json) as message_json,values(team_ref) as team_ref,values(HttpPath) as HttpPath,values(MessageBodyType) as MessageBodyType,values(HttpMethod) as HttpMethod,values(HttpStatusCode) as HttpStatusCode,values(dd_service) as dd_service,values(user) as user,values(MessageBody) as MessageBody,values(login_date) as login_date,values(login_entity_ref) as login_entity_ref,values(//other) as //other,values(Duration) as Duration,values(expiry_date) as expiry_date,values(Level) as Level,values(user_id) as user_id,values(session_ref) as session_ref,values(LogEntryCorrelationId) as LogEntryCorrelationId,values(c) as c,values(Uri) as Uri,values(dd_version) as dd_version,values(dvc) as dvc,values(Method) as Method,values(dd_env) as dd_env

|alter message_json = arrayindex(message_json,0),team_ref = arrayindex(team_ref,0),HttpPath = arrayindex(HttpPath,0),MessageBodyType = arrayindex(MessageBodyType,0),HttpMethod = arrayindex(HttpMethod,0),HttpStatusCode = arrayindex(HttpStatusCode,0),dd_service = arrayindex(dd_service,0),user = arrayindex(user,0),MessageBody = arrayindex(MessageBody,0),login_date = arrayindex(login_date,0),login_entity_ref = arrayindex(login_entity_ref,0),//other = arrayindex(//other,0),Duration = arrayindex(Duration,0),expiry_date = arrayindex(expiry_date,0),Level = arrayindex(Level,0),user_id = arrayindex(user_id,0),session_ref = arrayindex(session_ref,0),LogEntryCorrelationId = arrayindex(LogEntryCorrelationId,0),c = arrayindex(c,0),Uri = arrayindex(Uri,0),dd_version = arrayindex(dd_version,0),dvc = arrayindex(dvc,0),Method = arrayindex(Method,0),dd_env = arrayindex(dd_env,0)